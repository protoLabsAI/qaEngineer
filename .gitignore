.DS_Store
.proto/

# Hand-grounded review-findings dataset — local until it's big enough to move to
# protolabs (see protoLab tracking issue). Contains no secrets, just labels + claims.
data/review-eval/
